import CreateCampaign from "@/components/newsletter/CreateCampaign";

export const metadata = { title: "Create Campaign | NewsletterHub" };

export default function CampaignPage() {
  return <CreateCampaign />;
}
