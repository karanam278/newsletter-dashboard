import NewsletterHistory from "@/components/newsletter/NewsletterHistory";

export const metadata = { title: "Newsletter History | NewsletterHub" };

export default function HistoryPage() {
  return <NewsletterHistory />;
}
