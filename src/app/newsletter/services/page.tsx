import ManageServices from "@/components/newsletter/ManageServices";

export const metadata = { title: "Manage Services | NewsletterHub" };

export default function ServicesPage() {
  return <ManageServices />;
}
